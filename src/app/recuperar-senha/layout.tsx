export const metadata = {
  title: 'Recuperar Senha | EducaMaster AI',
  description: 'Recupere o acesso à sua conta na EducaMaster AI.',
};

export default function RecoverPasswordLayout({ children }: { children: React.ReactNode }) {
  return children;
}
