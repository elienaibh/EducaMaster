export const metadata = {
  title: 'Redefinir Senha | EducaMaster AI',
  description: 'Redefina sua senha para recuperar acesso à sua conta.',
};

export default function ResetPasswordLayout({ children }: { children: React.ReactNode }) {
  return children;
}
